# Lab2025
A folder with files in different languages to mess around. Primarly to remember how stuff works, to experiement and just have fun. 

Installation
1. Clone repository
2. Install dependencies

To add new dependencies use pip freeze > requirements.txt
To install these dependencies use pip install -r requirements.txt
